%body.bodyClass
  .header
    .panel
      .panel-default
        %h1 Hangman
        %p Do it or Die for it.
  .middle-page
    .col-md-5
      .small-panel
      .panel
        .panel-default
          -#= form_for(resource, as: resource_name, url: hangman_path(resource_name), html: { method: :post }) do |f|
          -#  = devise_error_messages!
          -#  %div
          -#    = f.label :email
          -#    %br/
          -#    = f.email_field :email, autofocus: true
          -#  %div= f.submit "Resend confirmation instructions"
          -#= render "users/shared/links"
          -#%h1 Login
          %br
          %div
          =form_tag :controller => :hangman, :action => :login do
            =label_tag 'Username'
            = text_field_tag 'username',nil,:placeholder=>'Username'
            %br
            %br
            =label_tag "Password"
            =password_field_tag 'password',nil,:placeholder=>'Password'
            %br
            %br
            =submit_tag "Login",{:class=>"btn btn-warning"}
            %input.submit-button{:type => 'button', :value => 'Cancel', :class=>'btn btn-warning'}
        %hr
        %p Note : If you are admin,enter admin's username and password otherwise you can log in as an user.
      .panel
        .panel-default
          .ErrorMessages
            =[:error].each_with_index do |key,i|
              %ul.list
                %li #{flash[key]}
                %li #{@uname}
      -#.panel
      -#  .panel-default
      -#
    .col-md-7
      .small-panel
      .panel
        .panel-default
          %h1 Sign Up
          %br
          %br
          =form_tag :controller => :hangman,:action => :create do |f|
            = text_field_tag 'fname',@posts.fname, :placeholder=>"Enter First name"
            = text_field_tag 'lname',@posts.lname, :placeholder=>"Enter Last name"
            %br
            %br
            = email_field_tag 'email', @posts.email,:placeholder=>"Enter Email Id",:class=>"text"
            %br
            %br
            = text_field_tag 'uname', @posts.uname,:placeholder=>"Enter Username",:class=>"text"
            %br
            %br
            = password_field_tag 'password', @posts.password,:placeholder=>"Enter Password",:class=>"text"
            %br
            %br
            = password_field_tag 'c_pass', nil,:placeholder=>"Confirm Password",:class=>"text"
            %br
            %br
            %input.submit-button{:type => 'submit', :value => 'Sign up', :class=>'btn btn-warning'}
            %input.submit-button{:type => 'button', :value => 'Cancel', :class=>'btn btn-warning'}
            %hr

  .row
  .footer
    .panel
      .panel-default
        .contact
          %p
            find code on :
            = link_to "https://github.com/kiranpawar87/Kiran_Project.git" do
              GitHub
          %p
            Website :
            = link_to "http://localhost:3000/hangman/index" do
              www.hangman.com
          %p &copy copyrights are reserved